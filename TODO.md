# TODO (BlackboxAI)

- [ ] Gather understanding of the cause of auto-inserted HTML (Emmet/IntelliSense snippets/format on type).
- [ ] Inspect existing `.vscode/settings.json` and any other VSCode config files.
- [ ] Plan changes to disable auto suggestions/snippet insertion and Emmet expansion (without blocking manual typing).
- [ ] Implement edits in `.vscode/settings.json` to prevent automatic `<div>` creation.
- [ ] Verify behavior by reloading VSCode (optional: restart TypeScript/HTML services).
- [ ] Run a quick sanity check: ensure manual typing still works and no other functionality is broken.
